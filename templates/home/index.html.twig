{% extends 'base.html.twig' %}
 {% block body %}
     <h1>Première page web avec Symfony</h1>
     <div class="container">
         {% for article in articles %}
             <h2>{{ article.title }}</h2>
         {% endfor %}
     </div>

 {% endblock %}